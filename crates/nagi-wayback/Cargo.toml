[package]
name = "nagi-wayback-foundation"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
sha2 = "=0.10.9"

# This contract/reference crate intentionally stays outside the product
# workspace until the 0.2 runtime integration gate is explicitly opened.
[workspace]
