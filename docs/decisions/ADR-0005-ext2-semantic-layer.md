# ADR-0005: ext2 and Semantic Layer

**Status:** Accepted

The persistent filesystem baseline is VFS plus ext2. Stable Object IDs are
maintained separately from paths and inode identity for the semantic layer.

