# Testing

Tests are layered as host unit tests, component tests, milestone acceptance
tests, and QEMU/integration tests. A milestone is `PASS` only after its build,
focused tests, and acceptance criteria have fresh evidence.

