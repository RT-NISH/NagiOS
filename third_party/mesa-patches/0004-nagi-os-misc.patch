diff --git a/src/util/os_misc.c b/src/util/os_misc.c
--- a/src/util/os_misc.c
+++ b/src/util/os_misc.c
@@ -57,7 +57,7 @@
 #  include <unistd.h>
 #  include <log/log.h>
 #  include <cutils/properties.h>
-#elif DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_MANAGARM
+#elif DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_MANAGARM || DETECT_OS_NAGI
 #  include <unistd.h>
 #elif DETECT_OS_OPENBSD || DETECT_OS_FREEBSD
 #  include <sys/resource.h>
@@ -332,6 +332,9 @@ os_get_total_physical_memory(uint64_t *size)
 #elif DETECT_OS_FUCHSIA
    *size = zx_system_get_physmem();
    return true;
+#elif DETECT_OS_NAGI
+   /* Nagi does not expose a physical-page query in this bootstrap slice. */
+   return false;
 #else
 #error unexpected platform in os_misc.c
    return false;
